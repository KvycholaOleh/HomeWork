package zoo;

import java.util.ArrayList;
import java.util.Collection;
import java.util.HashMap;
import java.util.Iterator;
import java.util.List;
import java.util.Map;
import java.util.Scanner;
import java.util.Set;
import java.util.Map.Entry;

public class TEst {
	

	public static void main(String[] args) {
		final Scanner sc = new Scanner(System.in);

		List<Pet> list = new ArrayList<>();
		Map<Person,List<Pet>> map = new HashMap<>();
		Set<Entry<Person,List<Pet>>> set = map.entrySet();
		Iterator<Entry<Person,List<Pet>>> iter = set.iterator();
		Person person1 = new Person(12, "asd", Gender.MALE);
		//Person person2 = new Person(12, "asdas", Gender.MALE);
		List<Pet> list1 = new ArrayList<Pet>();
		list1.add(new Pet("kowak", "cat", 1, Gender.FEMALE));
		map.put(person1,list1);
		System.out.println(map);
		System.out.println();
//		System.out.println("Enter member name");
//		String memberName = sc.next();

		Collection<List<Pet>> tmpList = map.values();
		Iterator it = tmpList.iterator();
		
	
		
//		while(iter.hasNext()){
//		Entry<Person, List<Pet>> keyValue = iter.next();
//		if (keyValue.getKey().getName().equals(memberName)) {
//			System.out.println("Enter pet name");
//			String petName = sc.next();
//			Iterator<Pet> it = keyValue.getValue().iterator();
//			while(it.hasNext()){
//				Pet pet = it.next();
//				if (pet.getName().equals(petName)) {
//					it.remove();
//				}
//				
//			}
//		}
//	}
		System.out.println(map);
		

			
			
			
			

	}

}
